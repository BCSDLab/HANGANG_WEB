import styled from 'styled-components';
import * as style from '@utils/css-util';

export const ReviewTitleContainer = styled.section`
  display: flex;
  align-items: center;
`;
export const ReviewTitleText = styled.h3`
  margin: 16px 5px 16px 16px;
  font-family: ${style.defaultFontFamily};
`;

export const ReviewTitleMetaData = styled.h2`
  font-size: 12px;
  font-weight: normal;
  color: #999999;
  margin-left: 10px;
`;
